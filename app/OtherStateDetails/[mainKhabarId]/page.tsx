"use client";
import React from "react";
import OtherStateDetails from "../../../components/OtherStateDetails";

type Props = {};

const page = (props: Props) => {
  return <OtherStateDetails />;
};

export default page;
