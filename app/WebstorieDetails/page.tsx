"use client";
import React from "react";
import WebstorieDetails from "../../components/WebstorieDetails";

type Props = {};

const page = (props: Props) => {
  return <WebstorieDetails />;
};

export default page;
