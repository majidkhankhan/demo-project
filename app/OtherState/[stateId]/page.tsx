"use client";
import React from "react";
import OtherState from "../../../components/OtherState";

type Props = {};

const page = (props: Props) => {
  return <OtherState />;
};

export default page;
