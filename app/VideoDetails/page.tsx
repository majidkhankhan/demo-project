"use client";
import React from "react";
import VideoDetails from "../../components/VideoDetails";

type Props = {};

const page = (props: Props) => {
  return <VideoDetails />;
};

export default page;
